<turbo-frame id="modal">  
  <% content_for :title, "Editing location" %>

  <h1>Editing location</h1>

  <%= render "form", location: @location %>

  <br>

  <div>
    <%= link_to "Show this location", @location, data: { turbo_frame: "modal" } %> |
    <%= link_to "Back to locations", locations_path, data: { turbo_frame: "_top" } %>
  </div>
</turbo-frame>